
# Binarios compilados. La entrada normal es `go run` (vía `npm run dev` o `make trazador-acceso`), que no
# deja binario; `npm run server:build` sí, y va a server/bin/.
/server/bin/
/trazador
# `go build ./...` corrido DENTRO de server/ deja un binario con el nombre de la carpeta.
/server/server

# Build de la Vue
/dist/
