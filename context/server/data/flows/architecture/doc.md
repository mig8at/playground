# Architecture · contexto
> **estado:** al día con main · Vista de ALTO NIVEL de cómo está armada la architecture de los repos de CreditOp — hoy 3 (application, legacy-backend, frontend-monorepo), extensible a otros.

<!-- STUB del árbol visual — la data (archivos) se linka en la fase de organización. -->

## Qué es
Vista de ALTO NIVEL de cómo está armada la architecture de los repos de CreditOp — hoy 3 (application, legacy-backend, frontend-monorepo), extensible a otros. Para un análisis rápido de cómo está construido cada repo (son un poco enredados). Contexto BASE: clave para casi cualquier tarea.

## Subcontextos
- **frontend-monorepo** — el front (wizard React).
- **legacy-backend** — el backend refactor (Laravel modular).
- **application** — el monolito viejo (Aliados) + servicing/admin.
- **MS Pre-approvals** — microservicio Go de pre-aprobación (rt≠0).

## Contenido
_Pendiente de curar._ La superficie de archivos y el detalle se linkan al organizar (la data curada previa vive en `context/server/data/flows-curated/` y en git `@159906a`).

## Bitácora
- **2026-07-17** — Nodo de contexto creado (stub) al armar el árbol visual del nuevo modelo contexto/task.

## Enlaces
- Raíz: **CreditOp**.
