# application · contexto
> **estado:** al día con main · Monolito Laravel 10/Inertia (repo `Creditop-SAS/legacy-application`). VIVO / default: lo que corre en prod hoy. Se organiza por **AUDIENCIA (subdominio)**, no por dominio; aloja en exclusiva el panel admin, el SSO de entrada y todo el servicing post-desembolso.

## Qué es
Monolito **Laravel 10 + Inertia + Vue 3/Vuetify** en un solo repo (`git@github.com:Creditop-SAS/legacy-application.git`, en disco `/Users/miguelochoa/Desktop/CREDITOP/github/legacy-application`). Escala: **~94.7k líneas PHP** en `app/` + **~90.9k líneas Vue/JS** en `resources/js`; 161 archivos de controller (160 controllers + la clase base), 169 models, 121 form requests, 41 Actions, 30 Jobs, 29 Services, 29 Notifications, 27 comandos de consola, 19 middleware, 311 páginas Inertia, 333 migraciones.

Es el **runtime por defecto** del ecosistema: aunque legacy-backend sea el destino de la migración, casi todo el flujo de originación sigue decidiéndose acá salvo las piezas explícitamente delegadas (registro de celular/OTP, KYC de documento, confirm de continuación). Este nodo cubre la **arquitectura del repo** — cómo está partido, qué aloja en exclusiva y por dónde se acopla; la mecánica de cada flujo vive en los nodos hermanos (Onboarding, CreditopX, Profiling, Merchants, KYC, Formalization).

**No es modular.** No hay `Modules/` ni bounded contexts: la unidad de organización es la **audiencia**, materializada como subdominio → grupo de middleware → namespace de controllers → archivo de rutas. De ahí se derivan casi todas sus rarezas: duplicación de controllers por audiencia, archivos de 1.700+ líneas y servicios compartidos entre back-office y cliente final.

## Antes de concluir
- ⚠ **«El admin no tiene equivalente en ningún otro repo» YA NO ES CIERTO, y creerlo hace planear mal el cutover.** Dos de las tres capas del panel ya están reconstruidas — la de **configuración** en `Modules/Partner` y la de **consulta** en `Modules/Backoffice` + `apps/backoffice`. Lo que NO tiene reemplazo en ningún lado es la **operación de plata**. El inventario medido, capa por capa, en §5.
- ⚠ **Lo que bloquea matar `application` es el RECAUDO, no el alta.** El alta ya se puede hacer por API; facturar, aplicar un pago, reversarlo o autorizar un desembolso solo existe acá — y es lo único que no se puede apagar sin que alguien deje de cobrar (§5).
- **HARDCODE Credifamilia (id 24)**: el accessor `Lender::getResponseTypeAttribute` (`app/Models/Lender.php:69-72`) fuerza `response_type = 1` ignorando la BD — y el cascade lo **vuelve a forzar** sobre el listado en `app/Services/lenders/LenderRetrievalService.php:226-231`. Son dos lugares a tocar para removerlo.
- **El perfilamiento y el orden del marketplace SOLO existen en producción**: `getProfilingData`, el paso de `demog_profiling_response` y `applyProfiling` + `usort` por `weighted_score` están gated a `app()->environment() === 'production'` (`app/Services/lenders/LenderRetrievalService.php:235`, `app/Services/lenders/LenderRetrievalService.php:244`, `app/Services/lenders/LenderRetrievalService.php:248-252`, `app/Services/lenders/LenderRetrievalService.php:282`). En local/dev el listado sale en **otro orden** y sin porcentajes de aprobación.
- **El ML NO está corto-circuitado en el código**: `ProfilerMLController::makePrediction:75` hace un `POST` real a H2O (timeout 15 s) y no hay ningún `return 404` en el archivo. Lo que pasa en runtime con la cadena de perfiladores: **→ `profiling`** (F-104).
- **Dependencia dura de legacy, sin fallback**: `app/Http/Controllers/Customer/RegisterCellPhoneController.php:181` `store` delega el registro de celular a legacy **incondicionalmente**, y su único `catch (\Throwable)` devuelve `back()->withErrors` (`app/Http/Controllers/Customer/RegisterCellPhoneController.php:276-291`). Si legacy no responde, no se puede iniciar ninguna solicitud. Contrasta con los bypass de OTP/laboral, que sí caen a la implementación local.
- **La superficie `api.` no tiene autenticación de grupo**: el grupo `api` solo trae throttle + bindings (`Kernel.php:82-86`); de las **41 rutas** definidas en `routes/api.php` solo **13** declaran `auth:sanctum` — recontado contra `main` el 2026-09-18 (el total bajó de 42 a 41; las autenticadas siguen siendo 13, así que la proporción empeoró levemente). `POST /voucher/regenerate` documenta explícitamente que "la key en base64 es la única barrera de seguridad" (`routes/api.php:36-38`); `corbeta/get-request` tiene su middleware comentado; `ApiTestController` (550 l) expone 14 rutas de prueba sin auth (batch de Experian, generación de datos para random forest, envío de mensajes de marketing) y hay dos closures `test-pullman` / `test-select-pullman` con ids quemados.
- ✅ **El registro de pagos de CreditopX en el admin quedó detrás de un permiso.** Desde el 2026-09-18, **cinco** rutas de `routes/admin.php` —registrar pago, reversarlo, registrarlo como pendiente y las dos de OTP de pago— exigen `can:register creditop x payments`. Antes cualquier usuario del back-office que alcanzara la ruta podía registrar un movimiento de plata sobre un crédito. Es la primera de las operaciones de cartera del panel con autorización explícita: el resto sigue dependiendo sólo de estar autenticado en el grupo `admin`.
- **Código muerto verificado** (0 referencias en todo el repo — rutas, PHP, Vue y tests): `Admin/AlliedRulesController.php` (237 l), `Admin/GroupRuleController.php` (14 l), `Customer/AdminCredentialsTestController.php` (65 l), `Customer/TermsController.php` (33 l), `Customer/UserSummaryController.php` (11 l). Middleware presente pero **no registrado** en `Kernel.php`: `RedirectIfRestricted` (guardas de orden del flujo de validación de identidad) y `OpenTelemetryMiddleware`.
- **Hardcodes de negocio en el camino caliente**: el parche *"TEMPORAL FIX ONLY FOR BANCOLOMBIA IN SONRIA"* pisa `fee_numbers`/`max_amount`/`rate` para `allied_id == 26 && lender 8` y está **duplicado** en `app/Http/Controllers/Customer/SimulatorController.php:73-78` y `app/Services/lenders/LenderRetrievalService.php:557-562`. Peor: la lista de Corbeta existe como Setting `corbeta_allieds` (`app/Http/Controllers/Customer/RegisterCellPhoneController.php:61`) **y** quemada como `[209,210,211]` en tres puntos del mismo archivo (`app/Http/Controllers/Customer/RegisterCellPhoneController.php:150`, `app/Http/Controllers/Customer/RegisterCellPhoneController.php:254`, `app/Http/Controllers/Customer/RegisterCellPhoneController.php:374`).
- **Buró mockeado fuera de prod**: `ExperianFixture.php` pesa 212 KB de respuestas sintéticas (+ `AgildataFixture`, `MareiguaFixture`); score y `additional_info` de dev no son reales.
- **Dos sistemas de CI conviven en el repo (fuera del índice de código, en la raíz)**: `bitbucket-pipelines.yml` mantiene el deploy legado por `git push` + `rsync` de `.env` y `public/build` a un volumen **EFS**, mientras `.github/workflows/main-prod.yaml` despliega por tag a ECR `creditop/legacy-application` en el cluster `inertia-production`, con **4 servicios**: `legacy-application`, `-worker`, `-worker-high`, `-scheduler`. La adaptación stateless ya está mergeada en main (`9d78e995 feature/stateless-ecs`): existe `/health`, `TrustProxies` lee `TRUSTED_PROXIES` (`app/Http/Middleware/TrustProxies.php:32-45`), el scheduler usa `onOneServer()->withoutOverlapping()` en todas las entradas, y `AGILDATA_CERT_PATH` ya es configurable (`config/services.php:64`).
- **La ruta en disco del doc sembrado quedó vieja**: el repo migró de Bitbucket a GitHub; ya no es `…/bitbucket/application` sino `…/github/legacy-application`.

## Contenido

### 1. El eje organizador: 5 audiencias por subdominio
`RouteServiceProvider::boot` (`app/Providers/RouteServiceProvider.php:45-77`) monta cinco grupos, cada uno con su dominio, su middleware, su namespace y su archivo de rutas:

| Subdominio | Rutas | Grupo (Kernel) | Namespace | Qué sirve |
|---|---|---|---|---|
| `api.{host}` | `routes/api.php` (182 l) | `api` — solo `ThrottleRequests:api` + `SubstituteBindings`, **sin auth** | `Api\` | webhooks entrantes de lenders/pasarelas (bancolombia, prami, meddipay, sistecredito, welli, payvalida, approbe, BdB, pash, self-manager, corbeta), ecommerce/VTEX, bulkload, endpoints de prueba |
| `admin.{host}` | `routes/admin.php` (207 l) | `admin` — `Authenticate` + `AuthenticateSession` + Inertia | `Admin\` | back-office: alta de entidad/comercio/sucursal, reglas, solicitudes, cartera CreditopX, reportes |
| `aliados.{host}` | `routes/customer.php` (404 l) | `customer` — Inertia + `RunModalSurveyLottery` + `NoIndex`, **auth opcional** | `Customer\` | **dos públicos en el mismo subdominio**: el asesor comercial autenticado (simulador, historial) y el cliente final anónimo (onboarding, OTP, formularios, listado, pagaré) |
| `perfil.{host}` | `routes/profile.php` (51 l) | `profile` | `Profile\` | perfil del cliente final, todo gated a móvil (alias `onlyMobile`) |
| *(default web)* | `routes/web.php` (72 l) | `web` | `Web\` | sitio público: home, TyC, privacidad, cookies, encuestas |

`/health` se registra **fuera de todo grupo y dominio** (`RouteServiceProvider.php:47`) porque el health check del ALB llega con la IP del task como `Host` y ningún grupo por subdominio matchearía. Grupos en `app/Http/Kernel.php:33-87`; los 18 alias (`onlyMobile`, `onlyWithUser`, `onlyMobileValidation`, `ecommerceNoData`, `no.index`…) en `app/Http/Kernel.php:96-115`.

**Consecuencia directa: la misma preocupación se duplica por audiencia.** 14 nombres de controller aparecen en ≥2 namespaces — `HomeController` en 4 (Admin/Customer/Profile/Web), `ValidateOtpController` y `OtpController` en 3, y `UserRequestController`, `CreditopXRequestHistoryController`, `UserController`, `PaymentLinkController`, `AuthorizationController`, `ProfilingReviewController`, `UserProfilingController`, `PurchaseCodeController`, `VideoTutorialController`, `ValidateIdentityController`, `PramiController` en 2. No son wrappers finos: `Customer/ValidateOtpController` = 601 líneas vs `Profile/` 103 vs `Admin/` 69; `Admin/CreditopXRequestHistoryController` = 1.767 vs `Customer/` 395.

### 2. Lo que aloja EN EXCLUSIVA
**(a) El panel admin — pero solo en parte; el inventario por capa está en §5.** Alta de entidad (`app/Http/Controllers/Admin/LenderController.php:210` `store` — siempre crea `credit_line_by_lenders` con `credit_line_id=1` en `app/Http/Controllers/Admin/LenderController.php:253`, y solo si rt==2 crea `CreditopXLenderConfiguration` en `app/Http/Controllers/Admin/LenderController.php:266`), de comercio (`app/Http/Controllers/Admin/AlliedController.php:99` `store`, con `allied_caterogy_id=1` en `app/Http/Controllers/Admin/AlliedController.php:111` y `new_screens=true` en `app/Http/Controllers/Admin/AlliedController.php:120` quemados) y de sucursal (`app/Http/Controllers/Admin/AlliedAlliedBranchController.php:239` `store` — genera `hash = hash('crc32', now)` en `app/Http/Controllers/Admin/AlliedAlliedBranchController.php:247` y sube el QR a S3 en `app/Http/Controllers/Admin/AlliedAlliedBranchController.php:253-254`). La **copia de reglas por sucursal** dispara en el *update* de la sucursal (`app/Http/Controllers/Admin/AlliedAlliedBranchController.php:207-208`, `addNewRule` + `addNewLenderRule`) y otra vez al crear credencial ecommerce (`app/Http/Controllers/Admin/AlliedEcommerceCredentialsController.php:98-99`). Mecánica completa en el nodo **Merchants**.

**(b) Todo el servicing / cobranza post-desembolso.** El scheduler (`app/Console/Kernel.php`) corre hoy **12 entradas activas de comando y ningún job** — contado contra `main` el 2026-09-18. ⚠ **Acá decía 19 + 1 job, y era cierto hasta el 2026-09-11**: ese día se comentaron —no se borraron— **ocho entradas y el job**, todas de REPORTERÍA, dejando activa sólo la conciliación de Corbeta. Se apagaron el semanal por comercio, el diario a comercios (con su WhatsApp de los lunes), el diario interno a admins, el semanal de créditos de consumo, el diario a Tuboleta, el de desembolsos por lender (sus dos entradas) y el job de bonificación de Credifamilia. **Dos de esos tienen destinatarios EXTERNOS** —Tuboleta y Credifamilia—, así que dejaron de recibir sin que el sistema avise. Las líneas quedan comentadas con la marca `[APAGADO 2026-09-11]` a propósito, para no perder qué se enviaba, a qué hora y con qué frecuencia. Seis son del ciclo de vida CreditopX: `00:02` pagos Wompi no aplicados, `00:10` saldos remanentes, `00:30` causación de intereses (`UpdateCreditopXRequestsCommand`, 381 l), `03:30` aplicación de pagos, `04:00` cupos rotativos, `09:30` recordatorios (`app/Console/Kernel.php:51-60`). El ledger es `creditop_x_requests_history` (`app/Models/CreditopXRequestHistory.php:14`). De los 27 comandos en disco, 9 no están agendados (los `creditopx:correct-negative-*` de reparación manual, `app:update-creditop-x-requests-n-times`, `app:invoice-process-confirm`, `app:update-initial-fee-payments`, y 2 comentados). **Evidencia de la exclusividad:** legacy-backend tiene copias de esos comandos, pero su `legacy-backend/app/Console/Kernel.php:13-19` solo agenda 4 (3 de device-lock MDM + `mark-stuck-crosscore-evaluations`) → las copias de servicing en legacy están muertas.

**(c) El SSO de entrada desde la app nueva.** `Customer/SsoCognitoController::login:23` recibe `access_token` + `ts` + `sig`, valida un **HMAC-SHA256** de `token|ts` contra `INTERNAL_SSO_TOKEN` con ventana de ±300 s (`app/Http/Controllers/Customer/SsoCognitoController.php:100-116`), llama a `https://{COGNITO_DOMAIN}/oauth2/userInfo`, mapea el usuario **por email** (`app/Http/Controllers/Customer/SsoCognitoController.php:67`), exige rol `Comercial` (`app/Http/Controllers/Customer/SsoCognitoController.php:74`) y aterriza en `simulator-v2` o `simulator` según `allied.new_screens` (`app/Http/Controllers/Customer/SsoCognitoController.php:149-156`). La ruta está exenta de CSRF (`app/Http/Middleware/VerifyCsrfToken.php:17`).

**(d) Reportería.** 20 clases en `app/Exports/` + jobs de envío por email. ⚠ Alimentan **las descargas del admin**, que siguen andando; **los envíos diarios y semanales del scheduler NO** — están apagados desde el 2026-09-11 (ver arriba). Las clases y los jobs siguen en disco, así que el código se lee como si el envío existiera: lo que dejó de existir es la agenda.

### 3. El acople hacia afuera: 4 vías + 3 interruptores
**Vías de salida** (base URL `INTERNAL_LEGACY_API_URL` vía `config('services.api.legacy_host')`, timeout 180 s en `app/Services/Api/BaseApiClient.php:22-32`):
- `IdentityApiClient` — KYC de documento: `/api/identity/validation/document{,/front,/back,/face-match}` y `/facial-recognition`.
- `LoansApiClient` — `/api/loans/customer/requests/confirm` (continuación) y `/api/loans/admin/users/{id}/manual-validation`. Es el único registrado como singleton (`app/Providers/ApiServiceProvider.php:15`).
- `GenerateServicesBridgeClient` — código de 4 dígitos contra `/api/onboarding/generate-services/code/consult` y `/code/consumConfirm` (`app/Services/Api/GenerateServicesBridgeClient.php:17-18`), con fallback a `host.docker.internal` bajo Sail (`app/Services/Api/GenerateServicesBridgeClient.php:293-315`).
- `LegacyLenderAttemptsService` — `/api/loans/admin/lenders/{id}/attempts`.
- Y `Http::` **directo** en controllers: `POST /api/onboarding/phone/register` (`Customer/RegisterCellPhoneController:187`, con `terms`/`policies` quemados en `true`).

`NewFrontendUrlService` no llama a nadie: **construye URLs** del wizard del frontend-monorepo (`NEW_FRONTEND_BASE_URL`) con tres prefijos (`merchant` | `ecommerce` | `self-service`, `app/Services/NewFrontendUrlService.php:8-14`) y los pasos `solicitar`, `personal-info`, `employment-info`, `lenders`, `imei`, más los callbacks de Bancolombia.

**Interruptores del parallel-run** — todos son filas de la tabla `settings` (`app/Models/Setting.php`, `value` casteado a JSON), no env ni feature flags:

| key | forma | qué conmuta | dónde se lee |
|---|---|---|---|
| `new_frontend_allied_branches` | `{"hashes":[…]}` | manda al usuario al **wizard nuevo** | `Customer/SimulatorController:121`, `Customer/UserRequestController:1498`, `HandleInertiaRequests:52` |
| `new_frontend_allieds` | `{"<alliedId>": true}` | idem, granularidad comercio | `SimulatorController:125`, `UserRequestController:1502` |
| `allowed_bypass_comerces` | `{"hashes":[…]}` + comodín `"all"` | **proxya a legacy** el otp-validate y la info laboral | `Customer/ValidateOtpController:111-122`, `Customer/PersonalInfoController:1022-1044` |

Los dos primeros redirigen el navegador (`inertia()->location()` si viene el header `X-Inertia`, si no `redirect()->away()`); el tercero mantiene al usuario en application y hace la llamada server-to-server, con `return $this->validateOtp($request)` / `storeEmploymentInfo(...)` como fallback tanto si el comercio no está en la lista como si legacy tira `RequestException`. Ambos bypass llevan el mismo comentario: *"todo: remove this optional bypass and the legacy methods when all tests are passed"*.

**Cuarto camino, desactivado:** la delegación del listado de entidades a `/api/onboarding/loan-application/lenders/{id}` está **comentada** en `Customer/ListLenderController.php:252`; `indexV2` corre el motor local (`$this->lenderRetrievalService->getLenders($userRequest)`, `app/Http/Controllers/Customer/ListLenderController.php:228`).

### 4. El motor de originación vive acá (detalle en nodos hermanos)
`app/Services/lenders/` (11 servicios, ~200 KB) es el corazón: `LenderRetrievalService::getLenders:73` (908 l, orquestador del cascade rt=2, instrumentado con `App\Otel\TracerService`), `PreApprovedLenderService` (775 l, despacho rt=1), `LenderUserCategoryService`, `LenderValidationService`, `ProfilingRulesService`, `RiskCentralValidationService`, `LenderSpecialGrantingService`, `LenderProbabilitySortingService`, `RevolvingLoanConfigService`. Los burós están en `app/Actions/RiskCentrals/` (Experian 24 KB + fixture, Tusdatos 31 KB, Agildata, Mareigua, Ado) y los 20 lenders externos en `app/Actions/Lenders/`. El perfilamiento demográfico es el archivo más grande del repo (`Admin/ProfilingController.php`, 4.770 l), seguido de la matriz (`Customer/Matrix/v1/MatrixModelController.php`, 2.064 l) y el ML (`ProfilerML/ProfilerMLController.php`).

### 5. El inventario de cutover: qué falta para matar `application`

Medido el 2026-08-08 comparando las **118 rutas** de `routes/admin.php` contra `Modules/Partner` +
`Modules/Backoffice` de legacy-backend y `apps/backoffice` del monorepo. La conclusión que reordena la
discusión: **el admin no es una cosa, son tres capas con tres estados de migración distintos.**

| capa del panel | en `application` | backend nuevo | front nuevo | estado real |
|---|---|---|---|---|
| **Configuración** — alta de comercio/entidad/sucursal, reglas duras y de datacrédito, credenciales de ecommerce, zonas, usuarios corporativos, productos | `Admin/Allied*`, `Admin/Lender*` (VIVO) | ✅ **`Modules/Partner`** — CRUD completo, `AlliedController@store` incluido | ❌ nada | **construido por delante de su cliente** |
| **Consulta / soporte** — usuarios, solicitudes, perfilamiento, scores Experian, validación manual, OTPs | `Admin/UserRequest*`, `Admin/ManualValidation*`, `Admin/Profiling*` (VIVO) | ✅ **`Modules/Backoffice`** (`cognito.token:staff`) | ✅ **`apps/backoffice`** (`users`, `loan-applications`, `staff-auth`) | **la única capa con las dos mitades** |
| **Operación de plata** — facturación y recaudo, aplicar/reversar pago, OTP de pago, links de pago, cupos rotativos, cadena de autorización | `Admin/CreditopXPayment*`, `RevolvingCreditPaymentManage*`, `CreditopXRequestHistory*`, `Authorization*` (VIVO) | ❌ **nada** | ❌ **nada** | **huérfana en los dos lados** |

**El bloqueador es la tercera fila, y pesa ~4.400 líneas** en siete controllers, con la cascada de
imputación entera adentro (`processPayment`, `processEarlyPayment`, `applyPayment`,
`principalReduction`, `reduceInstallment`, `applyRetainedPayments`, `waiveInterests`,
`reversePayment`). El recibo de que no hay reemplazo: `Modules/Payments` de legacy-backend solo expone
**links** de pago (`create`/`disable`/`process`/`return`), y grepear `reversePayment`,
`facturacion-y-recaudo` o `paymentManagement` en **todo** legacy-backend devuelve **cero**. La mecánica
de esa cascada es del nodo **`servicing`**, que es su dueño.

⚠ **La primera fila esconde una trampa de planificación.** El CRUD de alta de `Partner` **existe y no
tiene un solo cliente**: de todas sus rutas, el monorepo consume únicamente las del asesor
(`requests`, `user-requests`, `products`, `statistics`, `dynamic-form/session`) — el `POST` de
comercios, las reglas y las credenciales **no los llama nadie**. O sea que «el alta ya está migrada»
es cierto del backend y falso de punta a punta: apagar el panel de `application` hoy dejaría la
configuración **sin interfaz**, aunque la API esté lista. Es el mismo patrón de *construido por
delante de su cliente* que ya tienen los módulos V1 (→ `legacy-backend`).

**Cómo se lee este inventario para decidir:** la capa 2 se puede apagar cuando `apps/backoffice`
alcance paridad de pantallas; la capa 1 necesita **front nuevo, no backend**; la capa 3 necesita
**las dos mitades desde cero** — y es la que mueve dinero, así que es la que fija la fecha real.

**(2026-08-28) Re-verificación asistida de los 17 archivos derivados** (worker → 8; las 2 que
invalidaban, verificadas — ciertas): la regeneración de comprobantes ya es **ruta admin con sesión,
permiso y auditoría** (CORE-380) y el endpoint público quedó `@deprecated` (su única barrera es la
key); y **la exclusividad de crons de servicing en el monolito viejo se rompió**: el Kernel del NUEVO
agenda `app:reminder-creditop-x-requests-command` **a las 09:30 America/Santo_Domingo** — los
recordatorios de República Dominicana corren en legacy-backend (`Kernel.php:18`, verificado). La regla
«el servicing vive en application» ahora tiene UNA excepción con nombre. Más: condonación de saldos
residuales en rotativos, segmento de originación calculado, canal de onboarding write-once en el
registro de celular, y ciudades filtradas por país del comercio en el form de sucursales.

## Dónde mirar
- **El cutover** (§5): `legacy-backend/Modules/Partner/routes/api.php:18` (prefijo `merchants`) y `legacy-backend/Modules/Partner/routes/api.php:21` (`AlliedController@store`, el alta ya reconstruida) · `legacy-backend/Modules/Backoffice/routes/backoffice.php` (todo bajo `cognito.token:staff`) · `frontend-monorepo/apps/backoffice/app/routes.ts` (el alcance real del panel nuevo: users + loan-applications + staff-auth) · `application/routes/admin.php` (las 118 rutas que hay que reemplazar).
- **La capa huérfana** (solo application): `app/Http/Controllers/Admin/CreditopXPaymentController.php:48` (`store`), `app/Http/Controllers/Admin/CreditopXPaymentController.php:65` (`processPayment`, la cascada), `app/Http/Controllers/Admin/CreditopXPaymentController.php:1440` (`reversePayment`) · `Admin/CreditopXPaymentManageController.php` (facturación y recaudo) · `Admin/RevolvingCreditPaymentManageController.php` (facturación de rotativos) · `Admin/AuthorizationController.php` (la cadena tesorería/contabilidad/analista) · `Admin/PaymentLinkController.php`. Mecánica → **`servicing`**.
- **Boot / audiencias** (application): `app/Providers/RouteServiceProvider.php:45-77` (los 5 dominios + `/health`) · `app/Http/Kernel.php:33-87` (grupos) y `app/Http/Kernel.php:96-115` (alias) · `routes/{web,admin,customer,api,profile}.php`.
- **Parallel-run**: `app/Http/Controllers/Customer/SimulatorController.php:125-141` (allowlist → wizard nuevo) · `app/Http/Controllers/Customer/UserRequestController.php:1492-1614` (`validateTempUsers`, los 3 puntos de aterrizaje: personal-info / lenders / employment-info) · `app/Http/Controllers/Customer/ValidateOtpController.php:112-132` y `app/Http/Controllers/Customer/PersonalInfoController.php:1009-1071` (bypass a legacy con fallback) · `app/Http/Middleware/HandleInertiaRequests.php:50-55` (props compartidos al front).
- **Acople**: `app/Services/Api/BaseApiClient.php:22` · `app/Services/Api/GenerateServicesBridgeClient.php:17-18` · `app/Services/Api/IdentityApiClient.php` · `app/Services/Api/LoansApiClient.php` · `app/Services/NewFrontendUrlService.php:8-14` · `app/Services/LegacyLenderAttemptsService.php:11-31` · `app/Http/Controllers/Customer/RegisterCellPhoneController.php:179-229`.
- **Admin / alta** (exclusivo): `app/Http/Controllers/Admin/LenderController.php:210` · `app/Http/Controllers/Admin/AlliedController.php:99` · `app/Http/Controllers/Admin/AlliedAlliedBranchController.php:239` (store) y `app/Http/Controllers/Admin/AlliedAlliedBranchController.php:151-208` (update + copia de reglas) · `app/Http/Controllers/Admin/AlliedEcommerceCredentialsController.php:98` · `app/Http/Controllers/Admin/AlliedLenderController.php:49` (la calculadora por comercio; IVA forzado a 19 en `app/Http/Controllers/Admin/AlliedLenderController.php:128` y `app/Http/Controllers/Admin/AlliedLenderController.php:222`) · `app/Http/Controllers/Admin/AlliedModulesController.php:65` (escribe `status_per_profiles`) · `Admin/LenderRulesController.php` · `Admin/LenderDatacreditoRulesController.php`.
- **Servicing** (exclusivo): `app/Console/Kernel.php:13-69` (el calendario completo, todo con `onOneServer()->withoutOverlapping()`) · `app/Console/Commands/UpdateCreditopXRequestsCommand.php` · `…UpdateCreditopXApplyPaymentCommand.php` · `…UpdateCreditopXRevolvingCreditsCommand.php` · `…UpdateCreditopXRemoveOutstandingBalances.php` · `…UpdateCreditopXNotAppliedWompiPaymentCommand.php` · `…ReminderCreditopXRequestsCommand.php` · `app/Models/CreditopXRequestHistory.php` · `Admin/CreditopXRequestHistoryController.php` · `Admin/CreditopXPaymentController.php` · `app/Services/lenders/LenderCollectionChargeService.php`.
- **SSO**: `app/Http/Controllers/Customer/SsoCognitoController.php:23` (login), `app/Http/Controllers/Customer/SsoCognitoController.php:74` (rol `Comercial`), `app/Http/Controllers/Customer/SsoCognitoController.php:100-116` (HMAC + ventana 300 s), `app/Http/Controllers/Customer/SsoCognitoController.php:149-156` (destino).
- **Motor**: `app/Services/lenders/LenderRetrievalService.php:73` (entrada), `app/Services/lenders/LenderRetrievalService.php:226-231` (force Credifamilia), `app/Services/lenders/LenderRetrievalService.php:235-252` (gating de perfilamiento), `app/Services/lenders/LenderRetrievalService.php:491-505` (`getProfilingData`) · `PreApprovedLenderService.php` · `LenderUserCategoryService.php` · `app/Http/Controllers/Customer/ListLenderController.php:226-289` · `app/Http/Controllers/ProfilerML/ProfilerMLController.php:75` · `app/Otel/TracerService.php`.
- **Modelos base**: `app/Models/{Lender,Allied,AlliedBranch,LendersByAllied,LendersByAlliedBranch,UserRequest,User,Setting,StatusPerProfile}.php`. `AlliedBranch::hasCreditopX():83` es la compuerta que decide si la sucursal entra al flujo CreditopX; `User` usa `HasRoles` de spatie + `HasApiTokens`.
- **Front Inertia**: `resources/js/app.js` (resolve por `./pages/**/*.vue`, arranque con `window.inertiaPage`) · `vite.config.js` (Vuetify + SSR + auto-import + SRI en el manifest).

## Lo que NO está verificado
- Qué deploy sirve el tráfico hoy (EC2+EFS vs los 4 servicios Fargate de los workflows): sin verificar contra AWS.
- `allowed_bypass_comerces` es dato de BD: si contiene `"all"`, el otp-validate y la info laboral de TODOS los comercios ya corren en legacy.
