# ⚠ Este árbol NO lleva nodos-tarea

<!-- Plantilla retirada (partición del 2026-07-21): una tarea tiene estado, tiempo y una clave
     de Jira — todo eso vive en `tablero/data/<tarea>.md`, no acá. El árbol solo lleva contexto:
     lo que sigue siendo cierto después del merge. El enlace es unidireccional — la tarea apunta
     a nodos vía `context_nodes`; el nodo nunca apunta a tareas.

     Si estás por crear un nodo-tarea acá: pará y creá el archivo en el tablero.
     Al mergear, lo aprendido GRADÚA al nodo de contexto que corresponda. -->
