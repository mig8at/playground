# <Nombre> · contexto
> verificado contra `main` el <YYYY-MM-DD> — <MÉTODO: qué se leyó/corrió/midió para poder afirmarlo>
> <TL;DR: qué tema acotado cubre, en 1 frase>

<!-- CONTEXTO = una pieza de conocimiento acotada que puede tener SUBCONTEXTOS colgando.
     Contá solo SU tema; lo de los hijos vive en los hijos.

     El orden y las reglas de escritura completas están en `referencia.md` — son las mismas.
     Las dos que más cuestan si se ignoran: el `when` del map.json se escribe ANTES que el doc
     (es la interfaz que decide si el nodo se abre), y «Antes de concluir» va SEGUNDO porque un
     modelo llega con una hipótesis y necesita saber qué de ella es falso antes que la
     descripción. -->

## Qué es
<El tema en 1 párrafo, la conclusión primero.>

## Antes de concluir
<!-- Cada ítem corrige una conclusión obvia-y-falsa, o avisa de un riesgo vivo, CON su recibo
     (F-xx, commit, o el método con que se comprobó). Lo inferido se declara inferido. -->
- **<la regla>** — <la conclusión falsa que corrige, y con qué se comprobó>.

## <El cuerpo, en secciones ### por responsabilidad>

## Subcontextos <!-- (opcional) -->
- **<subcontexto>** — <qué cubre, en 1 línea; el contenido vive allá>.

## Dónde mirar
<!-- SOLO rutas con ancla y porqué — la lista completa ya vive en map.json. -->
- `repo/ruta/archivo.php:123` — <qué decide ahí>.

## Lo que NO está verificado <!-- (opcional) -->
<Con el método que falta. No preguntas hipotéticas.>
