# Citas que `simbolos.py` marca y que YA se verificaron a mano contra `origin/main`: la cita es
# correcta y lo que marca es el límite de la heurística, no un error del doc.
#
# ⚠ ESTA LISTA NO SE PUEDE PUDRIR EN SILENCIO. La clave es (nodo, ruta:línea exacta). Si alguien
# corrige esa cita, el número cambia, la entrada deja de coincidir y la herramienta AVISA que sobra
# —hay que volver a verificar y re-anotar—. Una entrada que perdona «el nodo X entero» sí se pudre;
# ésta no puede.
#
# Formato:  <nodo> <línea del doc> <ruta:línea>   # por qué se marca
# Revisadas el 2026-09-16 contra `origin/main`.

# ── el símbolo es de la frase de al lado (el corte por ` · ` y por punto no alcanza) ──
actors 40 routes/customer.php:109-111                                          # `shouldAuth` es de AuthController, citado aparte
actors 41 routes/profile.php:20-21                                             # `first_name TEMPORAL` es de LoginCellphoneController
actors 47 app/Http/Controllers/Admin/AlliedCorporateUserController.php:130-145  # `getFilteredIds` es de User.php
actors 140 Modules/Identity/App/Services/ManualValidationService.php:66-70      # `UserController@manualValidation` es la cláusula siguiente
aggregator 68 app/Actions/Allies/Corbeta.php:58                                # `EstadoOrden` va con `:131`
amount-tiers 18 Modules/Loans/App/Services/PromissoryNoteService.php:8         # `RegularPaymentScheduleService` es adonde NO está
bancolombia 66 app/Actions/Lenders/BancolombiaBnpl.php:525                     # `retrieve_quota` se nombra para decir que ahí NO hay sandbox
kyc 405 Modules/Identity/App/Services/IdentityValidationService.php:423        # `face_status` es el tramo siguiente
merchants 25 app/Http/Controllers/Admin/AlliedModulesController.php:77         # `status_per_profiles` es la consecuencia, no la línea
merchants 73 app/Http/Controllers/Admin/LenderRulesController.php:270          # `::updateOrCreate…` es la otra cita de la misma frase
merchants 115 Modules/Partner/App/Services/UrlGenerationService.php:27-58      # `AlliedManagementService` es el contraste («nadie los llama, y …»)
merchants 116 Modules/Partner/App/Services/AlliedManagementService.php:257-258 # `storeEcommerceCredential` es la cita siguiente
profiling 106 Modules/Loans/App/Services/LenderSpecialGrantingService.php:194-216 # `max_amount` es la frase siguiente
profiling 112 Modules/Onboarding/App/Services/lenders/ProfilingRulesService.php:13-19 # `adjustProbability` es la cita siguiente
smartpay 95 app/Models/DeviceLock.php:13-27                                    # `deviceLocks`/`activeDeviceLock` son de UserRequestProduct.php

# ── el nombre de la prosa NO es un identificador del archivo (tabla, columna, ruta HTTP) ──
merchants 202 app/Http/Controllers/Admin/AlliedModulesController.php:65        # `status_per_profiles` es una TABLA, no aparece en el controlador
merchants 215 app/Models/Allied.php:159                                        # el accessor se declara `getShowOriginatedCreditsAttribute`
onboarding 193 Modules/Onboarding/App/Services/CommerceService.php:127-130     # `allied_ecommerce_credentials` es una tabla; acá se decide por `isCommerceEcommerce`

# ── el símbolo está en el archivo, pero fuera de la ventana: lo usa un helper o una inyección ──
application 127 app/Services/lenders/LenderRetrievalService.php:73             # `getProfilingData` es otra cita de la misma frase
bancolombia 17 app/Actions/Lenders/BancolombiaBnpl.php:213                     # es el ÚNICO lugar de retrieveQuota que toca la clave
merchants 31 app/Http/Controllers/Admin/AlliedLenderController.php:239         # el nombre de la migración vive en legacy-backend, no acá
merchants 58 app/Http/Controllers/Admin/AlliedAlliedBranchController.php:183   # `LendersByAlliedBranch::…->delete()`; la tabla se nombra por el modelo
merchants 73 app/Http/Controllers/Admin/LenderRulesController.php:306          # ídem: recorre el modelo, no el nombre de la tabla
merchants 99 Modules/Onboarding/App/Http/Controllers/AlliedBranchController.php:48 # `business_name`/`store_name` se arman dentro de getByHash
merchants 109 Modules/AlliedBranchV1/App/Services/IsCorbetaOnboardingService.php:113 # `MANUAL_BIRTH_ALLIED_IDS` es de ManualPersonalDataAllieds
merchants 198 app/Http/Controllers/Admin/AlliedLenderController.php:128        # el `response_type == 2` es la OTRA cita de la frase (`:90`)
onboarding 194 Modules/Onboarding/App/Services/DynamicFormsService.php:546-568 # `UserRequestService` entra por inyección, declarado arriba
profiling 39 Modules/Loans/App/Services/LenderUserCategoryService.php:580      # `currentNegativeCredits` se compara dentro de validateOverdueAccounts

# ── la cita abarca un bloque y el símbolo describe el bloque entero ──
application 18 routes/api.php:36-38                                            # el comentario de la key en base64 ESTÁ ahí; `ApiTestController` es la frase siguiente
application 22 app/Http/Middleware/TrustProxies.php:32-45                      # `proxies()` lee `app.trusted_proxies`; los otros nombres son de otras citas
application 45 app/Console/Kernel.php:13-94                                    # el `schedule()` entero; los comandos se nombran sueltos
creditopx 209 database/seeders/ResponseTypesTableSeeder.php:24-35              # `smartpay_lender_id` es de Lender.php, citado aparte
formalization 129 Modules/Onboarding/App/Http/Controllers/ValidateOtpController.php:69-82 # `WelliRegistrationData` es la frase siguiente
frontend-monorepo 14 utils/auth/session.server.ts:5                            # `SESSION_SECRET`/`__session` son del OTRO archivo, citado a continuación
