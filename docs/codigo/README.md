# codigo/ — LA REALIDAD (verificada contra el código y la BD)

Cómo funciona CreditOp **HOY**: flujos, reglas, datos, hardcodes, migración y el admin real.
Todo lo de acá está **verificado** contra `application` / `legacy-backend` / `frontend-monorepo` / BD local, con citas `archivo:línea`.

- Si describe algo feo (copias de reglas, bypasses, `if id==N`), **no es una propuesta: es lo que hay**.
- Las mejoras viven en [`../mejoras/`](../mejoras/) y el destino en [`../vision/`](../vision/).
- Índice canónico con la descripción de cada doc: [`../CREDITOP.md`](../CREDITOP.md) §12.
